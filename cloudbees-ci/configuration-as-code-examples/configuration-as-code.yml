workspaceCache:
    cacheManager:
      googleCloudStorage:
        bucket: "bucket-name"
        credentialsId: "cred-id"
        prefix: "prefix"
    jobExcludes: "excluded-jobs"
    jobIncludes: "included-jobs"
  
